class Point {

    x : Int <- 5;
    y : Int <- x;
    a : String <- "abc";
    z : Bool <- false;
    b : Bool <- z;

    //b <- false;
    //x <- true;
    //y <- "hola";
    //a <- true;

    init(g : int, z : int) : IO {
        Moab : String;
        Moab <- "hola";
    };



    calculate() : Int {

    };

    calculate2() : Int {

    };


};

class Circle {

    abc : String;
    efg : Bool;

    x : Int;
    y : Int;

    init(g : int, z : int) : Circle {
        //x <- 10;
        //y <- 10;
    };

    calculate() : Int {
        lal : int;
    };

};

class Square inherits Circle {
    abc : String;
    efg : Bool;
    circulo : Circle;
    punto : Point;

	// abc : String;

    circulo.init(abc, efg);
    circulo.calculate();
    //point.calculate();
};

class Triangle inherits Square {

};


class main {

    main_circle : Circle;


    // main_square.init(10, 20);

    main_circle.init(10, 20);
    //main_circle.init(10,20);

    main() : void {
        num_test : int;
        num_test <- 100;
    };

    main.main(); // Inicializacion del programa

};

