class Point {

    x : Int;
    y : Int;
    a : String <- "abc";
    b : Bool <- true;

    b <- false;
    x <- true;
    y <- "hola";
    a <- true;

    init(g : int, z : int) : SELF_TYPE {
       //inside : String <- "Im Inside";
    };

    init(x, y);

};

class Circle inherits Point {

    abc : String;
    efg : Bool;

    x : Int;
    y : Int;
    init(g : int, z : int) : SELF_TYPE {
        x <- 10;
        y <- 10;
    };

};

class Square inherits Point {
    abc : String;
    efg : Bool;

	// abc : String;

};


class main {

    main_circle : Circle;

    main_square : Square;

    // main_square.init(10, 20);

    main_circle.init(10, 20);

    main() : main {

    };

    //new Main.main();

};

