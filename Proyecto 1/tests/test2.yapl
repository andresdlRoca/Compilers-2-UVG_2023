class Point {

    x : Int;
    y : Int;
    a : String <- "abc";
    b : Bool <- true;

    //b <- false;
    //x <- true;
    //y <- "hola";
    //a <- true;

    init(g : int, z : int) : Point {
        Moab : String;
        Moab <- "hola";
    };



    calculate() : Int {

    };

    calculate2() : Int {

    };


};

class Circle {

    abc : String;
    efg : Bool;

    x : Int;
    y : Int;

    init(g : int, z : int) : Circle {
        //x <- 10;
        //y <- 10;
    };

    calculate() : Int {
        calculate()
    };

};

class Square inherits Circle {
    abc : String;
    efg : Bool;
    circulo : Circle;
    punto : Point;

	// abc : String;

    circulo.init(abc, efg);
    circulo.calculate();
    //point.calculate();
};

class Triangle inherits Square {

};


class main {

    main_circle : Circle;


    // main_square.init(10, 20);

    main_circle.init(10, 20);
    //main_circle.init(10,20);

    main() : main {
        num_test : int;
    };

};

