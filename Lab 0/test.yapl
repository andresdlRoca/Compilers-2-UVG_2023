
class Main {
    x : String; 
    main : Int;
};
// Comentario

class Silly {
    copy() : SELF_TYPE { self }
};

// Clase de ejemplo
class ejemplo inherits Silly {
    zom : Int;
    kid : String;

    x : Service <- (new Service).copy();

    isNil() : Bool { False }
    
    // Comentario de prueba
    init(hd: Int, tl : List) : Cons {
        xcar <- hd;
    }

};
